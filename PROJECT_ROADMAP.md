# InternPath 待优化与未来发展方向

## 已补齐的基础项

- 增加一键启动脚本：`start_internpath.cmd`
- 增加一键停止脚本：`stop_internpath.cmd`
- 增加一键重启脚本：`restart_internpath.cmd`
- 增加统一控制脚本：`deploy/internpath.ps1`
- 旧的 `deploy/run_local.ps1` 已改为复用统一控制脚本，避免每次启动都强制创建虚拟环境和安装依赖。
- 补充 `pypdf` 到服务端依赖，和项目中已有的 PDF 文档解析能力对齐。
- 增加日志忽略规则，避免本地运行日志进入版本库。
- 增加根目录 `pytest.ini`，避免根项目测试误递归到 `ai-service` 并发生模块名冲突。

## 当前待优化

- 统一中文文案编码：部分历史文件存在乱码文本，建议后续集中转为 UTF-8 并人工校对界面文案。
- 完善依赖安装体验：当前启停脚本默认使用系统 `python`，后续可以增加独立安装脚本，专门处理虚拟环境和依赖安装。
- 增加启动前环境检查：检查 `uvicorn`、`npm`、`.env`、端口占用和模型配置，给出明确提示。
- 继续拆分 React 工作台组件和 FastAPI 路由，保持前后端边界清晰。
- 增强日志可观测性：把 Web 应用、AI 服务、用户操作、模型调用错误统一成结构化日志。
- 明确测试边界：根项目测试和 `ai-service` 测试应继续分开运行，并在文档中写清楚命令。

## 未来发展方向

- 建立完整的求职工作流：JD 收藏、简历版本、匹配分析、学习计划、模拟面试、投递追踪形成闭环。
- 强化 RAG 证据链：支持多简历、多项目材料、课程笔记、面试记录作为知识库来源。
- 加入报告质量门禁：对匹配分、证据覆盖率、幻觉风险、引用完整性设置可解释的质量阈值。
- 增加本地桌面集成：继续打通 AiSmartDrill，支持从分析结果一键生成练习、错题复盘和技能包。
- 做岗位数据沉淀：将牛客、B 站课程、薪资趋势和公司信息统一存储，支持可视化对比。
- 支持团队或班级场景：增加管理员视图、批量账号、班级任务、学习进度和报告导出。
