# 简历对话 Agent 真实流式传输与缓存修复方案

> 目标：用户提交分析后，10 秒内收到第一段真实 GPT 文本增量；重复输入能稳定命中本地缓存，并能观测 Provider prompt cache。
> 红线：不得使用前端占位消息、打字动画、固定文案或完整答案切片冒充模型流式回复。

## 一、验收定义

### 1. 真实流式

完整链路必须是：

~~~text
GPT Provider stream=True
  → BaseAgent 读取公开文本 delta
  → on_delta 回调
  → Graph 立即写入 model_delta 事件
  → SSE 长连接持续转发
  → 浏览器逐段追加真实文本
~~~

以下均不算第一条模型回复：

- SSE connected 注释；
- progress 事件；
- “正在分析”状态；
- typing 动画；
- 前端预置 assistant 消息；
- 完整答案返回后再切片播放。

### 2. 性能指标

所有端到端指标必须从数据库 run.created_at 开始计算。

| 指标 | 定义 | 目标 |
|---|---|---:|
| queueMs | run.started_at - run.created_at | P95 < 1,000ms |
| providerFirstTokenMs | Provider 请求发出到首个公开文本 delta | P95 < 8,000ms |
| endToEndFirstTokenMs | run.created_at 到首个 model_delta 落库 | P95 < 10,000ms |
| sseDeliveryLagMs | model_delta 落库到浏览器收到 | P95 < 500ms |

未命中本地结果缓存时，以首个真实 model_delta 作为 10 秒验收点。命中本地结果缓存时，以真实 suggestion 或 message 业务事件作为响应点，不得伪造 model_delta。

外部 Provider 的 TTFT 有波动，10 秒应作为 SLO 监控。若要求硬性保证，必须配置经过压测的快速模型和 Provider 故障切换，不能用假 token 掩盖超时。

## 二、已确认根因

1. 原 events 接口只查一次事件就发送 done，不会等待模型执行期间产生的新事件。
2. Advisor 调用 ResumeCopywriter 时没有贯通 on_delta，Provider token 被后端收集成完整文本后才落库。
3. 原流程在首条可见内容前串行执行 JD 模型解码、Draft 和 HR Critic。
4. Advisor 与普通长任务共用单 RQ worker，历史上出现过约 20 秒排队。
5. prompt hash 包含绝对路径，跨目录会失效。
6. Draft 输出受用户偏好影响，但旧缓存键没有完整偏好 fingerprint。
7. Provider cache usage 没有在每次调用后立即读取，下一次请求会覆盖前一次观测。
8. Provider 拒绝 retention/options 时，旧降级逻辑会连 prompt_cache_key 一并删除。
9. Responses 流解析可能把 function-call 参数或完整 done 文本混进公开回复。
10. SSE 每 120ms 同时查询事件、session 和 run，单客户端数据库查询过密。

## 三、目标架构

~~~mermaid
sequenceDiagram
    participant U as 用户
    participant API as FastAPI
    participant Q as Advisor Queue
    participant W as Advisor Worker
    participant P as GPT Provider
    participant DB as PostgreSQL
    participant S as SSE

    U->>API: 提交分析
    API->>DB: 创建 run 和 created_at
    API->>Q: enqueue internpath-advisor
    API-->>U: 返回 sessionId 和 runId
    U->>S: 建立事件长连接
    Q->>W: 专用 worker 接手
    W->>DB: RUNNING 和 started_at
    W->>P: stream=True
    loop 每个公开文本 delta
        P-->>W: output_text.delta
        W->>DB: append model_delta
        DB-->>S: 新事件
        S-->>U: model_delta
    end
    W->>DB: 事实核验、HR 评审、suggestion
    S-->>U: suggestion 和 done
~~~

## 四、后端改造

### 1. backend/agents/base.py

Responses API 必须按 typed event 解析：

- 只将 response.output_text.delta 作为公开文本；
- refusal delta 如需展示，应使用独立事件；
- function_call_arguments.delta 不得进入正文；
- reasoning 原文不得进入正文；
- output_text.done 不得重复追加完整答案；
- response.completed 仅在整个流完全没有文本 delta 时，才可作为兼容性兜底。

Chat Completions 路径只读取 choices[].delta.content。

on_delta 必须在每次读取到公开文本时立即调用，不能等流结束。

Provider client 应在 worker 进程内复用，复用 HTTP 连接池，减少重复 DNS、TCP 和 TLS 建连。

每次 Provider 请求结束后立即保存 usage，包括：

- cached input tokens；
- cache write tokens；
- Provider 是否上报缓存；
- 实际使用的 prompt cache 参数；
- Provider TTFT。

### 2. backend/agents/resume_copywriter.py

rewrite_section 接收到 on_delta 时，必须直接调用真实流式接口：

~~~python
await self._call_llm_stream(
    system_prompt=system_prompt,
    user_prompt=user_prompt,
    on_delta=on_delta,
    temperature=temperature,
)
~~~

禁止调用非流式接口后再人工切片。

首轮 prompt 只保留：

- 稳定系统规则；
- 当前目标简历段落；
- 精简后的 JD requirements；
- 已确认用户偏好；
- 直接输出建议文本的要求。

首轮不要包含整份日志、无关历史消息和重复 JSON。

### 3. backend/resume_advisor/verification.py

执行顺序改为：

1. JD 首轮只做确定性本地抽取或读取本地缓存。
2. ResumeCopywriter 成为第一个 Provider 调用。
3. Draft 透传 on_delta、on_cache_event 和 on_provider_usage。
4. Draft 完整返回后再做事实核验。
5. 最后执行 HR Critic。
6. 本地 Draft 缓存命中时直接返回缓存结果，不发布伪 model_delta。

HR Critic 可以影响最终 suggestion，但不能阻塞第一段 GPT 文本。

### 4. backend/resume_advisor/graph.py

建立真实 delta 写入器：

1. 第一个非空 delta 立即写入数据库，不做字符阈值缓冲。
2. 后续 delta 可按最多 50 至 100ms，或累计 32 至 64 字符合并。
3. 模型流结束或异常退出前强制 flush。
4. 每个 model_delta 只包含公开内容：

~~~json
{
  "delta": "真实文本增量",
  "stage": "draft_suggestion",
  "agent": "resume_copywriter",
  "modelId": "实际模型",
  "blockId": "目标块 ID"
}
~~~

第一个 model_delta 额外包含：

~~~json
{
  "providerFirstTokenMs": 1234,
  "queueMs": 180,
  "endToEndFirstTokenMs": 1810,
  "firstTokenSloMs": 10000,
  "firstTokenSloMet": true
}
~~~

同时把这些指标合并到 agent_resume_runs.telemetry_json，供统计和告警使用。

模型异常时，先 flush 已收到的真实 delta，再发布 error，并把 run 标记为 FAILED。

### 5. 真实工具事件

只在真实函数执行前后发送工具事件：

~~~text
tool_call: extract_jd_requirements
tool_result: extract_jd_requirements
tool_call: verify_suggestion_facts
tool_result: verify_suggestion_facts
tool_call: hr_quality_review
tool_result: hr_quality_review
~~~

tool_call 示例：

~~~json
{
  "callId": "唯一 ID",
  "toolName": "verify_suggestion_facts",
  "stage": "fact_verification",
  "agent": "resume_advisor",
  "blockId": "目标块 ID"
}
~~~

tool_result 示例：

~~~json
{
  "callId": "同一 ID",
  "toolName": "verify_suggestion_facts",
  "ok": true,
  "durationMs": 12,
  "summary": {
    "status": "supported",
    "issueCount": 0
  }
}
~~~

工具事件不得包含完整简历、完整 JD、API Key、隐藏 prompt、Provider 原始 reasoning 或完整工具参数。

## 五、SSE 长连接

backend/resume_advisor/router.py 的 events 接口必须：

1. 建连时只校验用户和 session 一次。
2. 立即发送 connected 注释，但不计入模型回复。
3. 根据 afterSequence 持续读取新事件。
4. run 为 QUEUED 或 RUNNING 时绝不发送 done。
5. 只在 COMPLETED、FAILED、PAUSED 等终态后补拉一次最终事件，再发送 done。
6. 每 15 秒发送 keep-alive。
7. 检测客户端断开并退出生成器。
8. 返回以下响应头：

~~~http
Content-Type: text/event-stream
Cache-Control: no-cache, no-transform
X-Accel-Buffering: no
Connection: keep-alive
~~~

轮询频率建议：

- 新事件：100 至 150ms；
- run 状态：500 至 1,000ms；
- session 权限：仅入口校验一次。

中期应迁移为 PostgreSQL 持久化加 Redis Stream 通知，SSE 使用 XREAD BLOCK，减少数据库高频轮询。

生产反向代理需要关闭缓冲：

~~~nginx
location /api/agent/resume/ {
    proxy_buffering off;
    proxy_cache off;
    gzip off;
    proxy_read_timeout 3600s;
}
~~~

## 六、独立 Advisor Worker

环境变量：

~~~env
RQ_QUEUE_NAME=internpath-default
RQ_ADVISOR_QUEUE_NAME=internpath-advisor
~~~

至少运行两个独立进程：

~~~text
Worker A：只监听 internpath-default
Worker B：只监听 internpath-advisor
~~~

Advisor worker 不得同时监听 default，否则普通长任务仍会造成队头阻塞。

需要同步检查：

- config.py；
- backend/task_queue.py；
- backend/rq_worker.py；
- Windows 启停脚本；
- start_internpath.sh；
- stop_internpath.sh；
- deploy/systemd/internpath-worker.service；
- deploy/systemd/internpath-advisor-worker.service；
- .env.example 和服务器环境变量示例。

10 秒预算：

| 阶段 | 最大预算 |
|---|---:|
| API 创建 run 和 enqueue | 300ms |
| Advisor queue 等待 | 700ms |
| Worker 读取上下文 | 500ms |
| Provider 首 token | 8,000ms |
| 落库和 SSE 转发 | 500ms |
| 合计 | 10,000ms |

控制措施：

- worker 启动时初始化并复用 Provider client；
- 首轮禁止调用 JD Decoder 模型；
- 首轮禁止等待 HR Critic；
- 首轮只读取当前段落和必要证据；
- 设置明确的 connect 和 read timeout；
- 低延迟 reasoning 参数只能在当前模型官方支持时启用；
- 超时记录真实失败，禁止用伪流掩盖。

## 七、缓存修复

### 1. 本地结果缓存

不同阶段使用独立 namespace：

~~~text
resume_advisor_jd_decode_v1
resume_advisor_draft_v1
resume_advisor_quality_v1
~~~

Draft 缓存键至少包含：

- model ID；
- schema version；
- tool 或 prompt version；
- 原始段落规范化内容或 hash；
- section；
- JD requirements；
- revision feedback；
- 用户偏好 fingerprint；
- 影响输出的模型参数。

缓存键不得包含：

- 绝对安装路径；
- 临时时间戳；
- run ID；
- 无业务必要的 session ID；
- 无关日志。

相同有效输入必须得到相同 key。任何会改变输出的偏好、模型、schema 或 prompt 版本必须使缓存失效。

### 2. Provider Prompt Cache

1. 稳定系统协议放在 instructions 最前部。
2. 动态用户内容放在稳定前缀之后。
3. 使用稳定 prompt_cache_key。
4. retention/options 只在当前模型官方支持时发送。
5. Provider 拒绝 retention/options 时，第一次降级只删除这些字段，保留 prompt_cache_key。
6. Provider 仍明确拒绝 key 时，才删除 prompt_cache_key。
7. 每次调用后立即读取 cached tokens 和 cache write tokens。

Provider prompt cache 通常要求稳定前缀达到最低 token 阈值。短 prompt 未命中可能是正常行为，验收用例必须使用足够长且完全一致的稳定前缀。

### 3. 缓存观测事件

本地缓存：

~~~json
{
  "type": "cache",
  "payload": {
    "namespace": "resume_advisor_draft_v1",
    "hit": true,
    "stage": "cache_lookup"
  }
}
~~~

Provider cache：

~~~json
{
  "type": "provider_usage",
  "payload": {
    "agent": "resume_copywriter",
    "modelId": "实际模型",
    "providerCacheAvailable": true,
    "providerCacheHit": true,
    "providerCachedTokens": 2048,
    "providerCacheWriteTokens": 0
  }
}
~~~

本地结果缓存和 Provider prompt cache 必须分开展示和统计。

## 八、前端边界

前端只负责消费真实事件，不负责制造“快”的假象。

允许：

- 收到 model_delta 后逐段追加真实文本；
- 状态条显示公开阶段、真实工具名、缓存和耗时；
- done 后拉取一次 snapshot 对账；
- EventSource 断线后携带最后 sequence 重连。

禁止：

- 提交后立即插入假 assistant 消息；
- 用 typing 动画计入首条回复；
- 把 progress 当作模型回复；
- 将完整答案人工切片；
- 展示私有 reasoning、隐藏 prompt 或工具参数；
- 每个 SSE 事件都重新请求完整 snapshot 和 resume-view。

状态条必须位于对话列表外，不进入对话历史。

## 九、测试与真实验收

### 1. 后端单元测试

- Provider 首个 callback 到达时，Graph 已经写入 model_delta，Draft 尚未返回。
- 第一个 model_delta 不受字符阈值缓冲。
- endToEndFirstTokenMs 从 run.created_at 计算并包含 queue time。
- firstTokenSloMet 在 10 秒边界计算正确。
- function-call arguments 不进入公开 delta。
- output_text.done 不重复正文。
- retention/options 被拒时仍保留 prompt_cache_key。
- 相同输入第二次本地缓存 hit。
- 用户偏好变化使 Draft cache key 变化。
- 绝对路径变化不影响 prompt hash。
- 缓存 Draft 不产生伪 model_delta。
- 事实核验和 HR 评审有成对的真实 tool_call 和 tool_result。

### 2. SSE 集成测试

- run 为 QUEUED 或 RUNNING 且暂时无事件时，连接不结束。
- 后续写入的 model_delta 能被同一连接收到。
- sequence 单调递增。
- afterSequence 重连不重复消息。
- 终态前补拉最终事件，再发送 done。
- 响应头正确关闭代理缓冲。

### 3. RQ 测试

- Advisor job 只进入 internpath-advisor。
- 默认 worker 只监听 internpath-default。
- Advisor worker 只监听 internpath-advisor。
- 启动脚本能拉起两个不同 PID。
- 停止脚本能分别关闭两个 worker。

### 4. 真实环境验收

必须使用真实 Provider、Redis、PostgreSQL、浏览器和反向代理：

1. 重启全部服务。
2. 确认有两个独立 RQ worker。
3. 提交一份未缓存的简历和 JD。
4. 记录 POST、SSE 建连、worker started_at、首个 model_delta.created_at 和浏览器收到时间。
5. 验证 endToEndFirstTokenMs 小于 10,000ms。
6. 完全相同输入再次提交，验证本地缓存 hit。
7. prompt 足够长时验证 Provider cached tokens。
8. 修改一项用户偏好，确认 Draft 本地缓存失效。
9. 确认状态条不在对话 DOM 容器内，且没有私有 reasoning。

## 十、Todo List

### P0：真实流式主链

- [x] BaseAgent 按 typed event 解析。
- [x] ResumeCopywriter 透传 on_delta。
- [x] Graph 首 delta 立即落库。
- [x] SSE 保持长连接并持续转发。
- [x] 删除假 typing 和假分片逻辑。

### P0：10 秒 SLO

- [x] Advisor 独立队列。
- [x] Advisor 独立 worker。
- [x] Copywriter 成为首个 Provider 调用。
- [x] 首轮 JD 只走本地解析或缓存。
- [x] 记录 queue、Provider TTFT 和端到端 TTFT。

### P1：缓存

- [x] 修复稳定 cache key。
- [x] 加入用户偏好 fingerprint。
- [x] 分阶段 namespace、schema 和 tool version。
- [x] Prompt cache 分级降级。
- [x] Provider usage 每次调用后立即采集。

### P1：公开运行状态

- [x] 发布真实 tool_call 和 tool_result。
- [x] 状态条显示公开阶段、缓存和耗时。
- [x] 状态条不进入对话历史。

### P2：扩展性

- [x] SSE 通知迁移到 Redis Stream。
- [x] 增加 Advisor worker 自动扩缩容。
- [x] 建立首 token SLO 仪表盘和告警。

## 十一、完成标准

- [x] Provider 请求真实使用 stream=True。
- [x] 首个 Provider 文本 delta 在模型完成前写入数据库。
- [x] 同一 SSE 连接实时收到该 delta。
- [x] 前端没有用占位或人工切片冒充模型回复。
- [ ] P95 endToEndFirstTokenMs < 10,000ms。
- [ ] P95 queueMs < 1,000ms。
- [x] Advisor 与 default worker 进程隔离。
- [x] 相同有效输入的本地缓存可稳定命中。
- [x] Provider cache usage 可观测。
- [x] 缓存命中不伪造 Provider delta。
- [x] function-call 参数和私有 reasoning 不进入对话。
- [x] 工具状态来自真实函数调用。
- [x] 后端测试、前端测试和生产构建通过。
- [ ] 真实浏览器与反向代理验收通过。

## 十二、验证命令

~~~powershell
.\.venv\Scripts\python.exe -m pytest tests/test_resume_advisor_api.py tests/test_resume_advisor_graph.py tests/test_resume_advisor_verification.py tests/test_agent_cache.py tests/test_model_responses_routing.py tests/test_rq_worker.py -q
Set-Location frontend
npm run test
npm run build
~~~

服务重启后必须继续检查真实 RQ worker 队列、Provider usage、SSE 时序和运行日志。仅单元测试通过，不能证明 10 秒真实首 token 已达成。
