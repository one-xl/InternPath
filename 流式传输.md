# 后端 Agent 的 GPT 流式传输接入指南

## 目标

让网页中展示的后端 agent 支持 GPT 流式输出，也就是模型一边生成，页面一边显示，而不是等完整回答生成完再一次性返回。

推荐链路：

```text
浏览器页面 -> 你的后端 agent 接口 -> OpenAI GPT 流式接口 -> 后端转发流 -> 浏览器逐段渲染
```

不要让浏览器直接调用 OpenAI API。API Key 应只保存在后端环境变量中。

## 推荐方案：SSE 或 fetch stream

如果只是“后端不断输出文字，前端不断显示”，优先使用 SSE 风格的文本流。

常见选择：

- `fetch + ReadableStream`：支持 `POST`，适合聊天、agent 提问、携带复杂参数。
- `EventSource`：浏览器原生 SSE 客户端，但默认只支持 `GET`。
- `WebSocket`：适合双向实时交互，例如用户中途打断、多路事件、复杂工具调用状态。

大多数 agent 页面用 `fetch + ReadableStream` 就够了。

## 后端实现：Node.js / Express

安装依赖：

```bash
npm install express openai
```

后端示例：

```js
import express from "express";
import OpenAI from "openai";

const app = express();
const client = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

app.use(express.json());

app.post("/api/agent/stream", async (req, res) => {
  const { message } = req.body;

  res.setHeader("Content-Type", "text/event-stream; charset=utf-8");
  res.setHeader("Cache-Control", "no-cache, no-transform");
  res.setHeader("Connection", "keep-alive");
  res.setHeader("X-Accel-Buffering", "no");
  res.flushHeaders?.();

  req.on("close", () => {
    // 用户关闭页面或取消请求时会触发。复杂场景可在这里中止上游请求。
  });

  try {
    const stream = await client.responses.create({
      model: process.env.OPENAI_MODEL,
      input: message,
      stream: true,
    });

    for await (const event of stream) {
      if (event.type === "response.output_text.delta") {
        res.write(
          `data: ${JSON.stringify({
            type: "delta",
            text: event.delta,
          })}\n\n`,
        );
      }

      if (event.type === "response.completed") {
        res.write(`data: ${JSON.stringify({ type: "done" })}\n\n`);
        res.end();
      }
    }
  } catch (error) {
    res.write(
      `data: ${JSON.stringify({
        type: "error",
        message: error instanceof Error ? error.message : "stream failed",
      })}\n\n`,
    );
    res.end();
  }
});

app.listen(3000, () => {
  console.log("Agent server listening on http://localhost:3000");
});
```

环境变量示例：

```bash
OPENAI_API_KEY=你的 OpenAI API Key
OPENAI_MODEL=按你的账号可用模型填写
```

## 前端实现：fetch 读取流

HTML：

```html
<div id="answer"></div>
```

JavaScript：

```js
async function askAgent(message) {
  const answerEl = document.querySelector("#answer");
  answerEl.textContent = "";

  const response = await fetch("/api/agent/stream", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ message }),
  });

  if (!response.body) {
    throw new Error("当前浏览器不支持 ReadableStream");
  }

  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  let buffer = "";

  while (true) {
    const { value, done } = await reader.read();
    if (done) break;

    buffer += decoder.decode(value, { stream: true });

    const chunks = buffer.split("\n\n");
    buffer = chunks.pop() || "";

    for (const chunk of chunks) {
      if (!chunk.startsWith("data: ")) continue;

      const payload = JSON.parse(chunk.slice(6));

      if (payload.type === "delta") {
        answerEl.textContent += payload.text;
      }

      if (payload.type === "done") {
        console.log("stream completed");
      }

      if (payload.type === "error") {
        console.error(payload.message);
      }
    }
  }
}
```

调用：

```js
askAgent("帮我分析这个岗位是否适合计算机专业实习生");
```

## 如果后端 Agent 用的是 Agents SDK

如果你的 agent 已经不是直接调用 `client.responses.create`，而是使用 OpenAI Agents SDK，那么思路不变：

```text
Agents SDK stream -> 后端遍历事件 -> 转成 data: {...}\n\n -> 前端渲染
```

也就是说，后端不需要把 SDK 的原始事件完整暴露给前端。建议在后端统一转换成你自己的事件格式：

```json
{ "type": "delta", "text": "部分文本" }
{ "type": "tool_start", "name": "工具名" }
{ "type": "tool_end", "name": "工具名" }
{ "type": "done" }
{ "type": "error", "message": "错误信息" }
```

这样前端不会被具体 SDK 事件结构绑定，后续替换模型或 agent 框架更容易。

## 前端渲染建议

最小实现是直接拼接文本：

```js
answerEl.textContent += payload.text;
```

如果需要 Markdown 效果，建议先累积完整文本，再交给 Markdown 渲染器：

```js
let fullText = "";

if (payload.type === "delta") {
  fullText += payload.text;
  answerEl.innerHTML = marked.parse(fullText);
}
```

如果答案很长，可以做节流渲染，避免每个 token 都触发一次 DOM 更新。

## 中断生成

前端可以用 `AbortController` 取消请求：

```js
const controller = new AbortController();

fetch("/api/agent/stream", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ message }),
  signal: controller.signal,
});

// 用户点击停止按钮时调用
controller.abort();
```

后端可以监听连接关闭：

```js
req.on("close", () => {
  console.log("client disconnected");
});
```

复杂场景下，可以把这个信号继续传给上游模型请求或 agent 运行器。

## 部署注意事项

流式传输最常见的问题不是代码，而是中间层缓冲。

需要检查：

- 后端响应头是否设置了 `Content-Type: text/event-stream`
- 是否设置了 `Cache-Control: no-cache, no-transform`
- Nginx 是否关闭响应缓冲
- Vercel、Netlify、Cloudflare、网关层是否支持长连接或流式响应
- 前端是否真的在读 `response.body.getReader()`
- 后端是否在循环中持续 `res.write(...)`，而不是收集完再 `res.json(...)`

Nginx 可参考：

```nginx
location /api/agent/stream {
  proxy_pass http://localhost:3000;
  proxy_http_version 1.1;
  proxy_set_header Connection "";
  proxy_buffering off;
  proxy_cache off;
  gzip off;
}
```

## 推荐落地顺序

1. 先在本地跑通 `/api/agent/stream`。
2. 用 `curl -N` 验证后端是否真的逐段输出。
3. 再接入前端 `fetch + reader`。
4. 最后处理停止生成、错误展示、工具调用状态、Markdown 渲染。

验证命令：

```bash
curl -N -X POST http://localhost:3000/api/agent/stream \
  -H "Content-Type: application/json" \
  -d "{\"message\":\"写一段流式输出测试文本\"}"
```

如果 `curl -N` 能一段一段显示，但网页不行，问题多半在前端读取逻辑。

如果 `curl -N` 也等到最后才显示，问题多半在后端或代理缓冲。

## 参考文档

- OpenAI Responses streaming events: https://platform.openai.com/docs/api-reference/responses-streaming
- OpenAI Agents SDK streaming: https://openai.github.io/openai-agents-js/guides/streaming/
